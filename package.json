{
  "name": "nano-router",
  "module": "src/Router.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --watch ./src/Router.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vitest": "^2.0.5"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}

